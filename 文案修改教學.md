# 自己修改 LINE 機器人的文案

## 只改這一個檔案

在 VS Code 左邊找到 **copy.zh-TW.json**，點開後即可修改，不需要改程式。

1. 找到你想改的項目，例如 `listTitle` 目前是「最近班次」。
2. 只修改右邊雙引號內的文字，例如改成「下一班搭這些 🚆」。
3. 按 **Ctrl + S** 儲存。
4. 在 LINE 重新傳完整的「回程」，下一則回覆就會使用新版文案。已送出的歷史訊息不會改變，不必重新啟動後端。

例如：

```json
"listTitle": "下一班搭這些 🚆"
```

不要刪除項目之間的逗號、雙引號或大括號。格式錯誤時，執行中的服務會繼續使用上一個有效版本；重啟時若檔案仍錯誤，會用程式內建版本。

## 常用項目

| 項目 | 顯示位置 |
| --- | --- |
| `header` | 列表最上方的路線 |
| `listTitle` | 最近班次標題 |
| `trainRow` | 每班車的出發時間、車種及車次 |
| `now` | 現在時間 |
| `listHint` | 提醒選擇 1／2／3 |
| `arrival` | 選擇班次後的整段訊息 |
| `buttonBoarded` | 已搭上按鈕的文字 |
| `boarded` | 按「已搭上」後的文字 |
| `buttonAcknowledged` | 知道按鈕的文字 |
| `acknowledged` | 家人按「知道」後的簡短回覆 |
| `eta` | 估計抵達時間與剩餘分鐘 |
| `buttonMissed` | 沒搭上按鈕的文字 |
| `missed` | 按「沒搭上」後的文字 |

選班、搭上與沒搭上的最後一行若維持 `【……】` 格式，Bot 會用 Flex Message 將該行顯示為粗體；若 LINE 用戶端未呈現粗體，就維持一般顯示，不使用其他替代方案。
| `missedTitle` | 沒搭上時的開頭，也用於查不到下一班的提示 |

`[ ... ]` 內一行是一段文字，空字串 `""` 表示空白行。按鈕文字最多 20 個 UTF-16 字元；一般中文字通常算 1 個，部分 emoji 算 2 個。

`{from}`、`{to}`、`{arrival}`、`{remaining}`、`{delay}` 等是程式填入的變數。**請保留原本項目的變數名稱，不要翻譯或自行創造變數。** 例如 `{to}` 會變成「大湖」，`{delay}` 會變成誤點分鐘數。

已保留指定的列表、選車、已搭上與沒搭上文案。`{etaDisplay}` 是抵達時間，`{etaShort}` 會把 `05:44` 顯示為 `5:44`，跨日會加上日期。`{direction}` 會依選擇顯示「去程」或「回程」。`listHint` 和 `scheduleNote` 可設為空字串。

按「沒搭上」只顯示下一班的出發與預估抵達時間，「沒搭上」按鈕也改為對應下一班。沒有後續班次或查詢失敗時會顯示提示，不會填入示範時間。

如要檢查格式並看人工資料預覽，可在專案終端機執行：

```powershell
.\run.ps1 copy-check
```

這個檢查不會發送 LINE 訊息，也不需要金鑰。

## 文字與指令是分開的

改按鈕顯示文字不會改變程式內部指令。手動輸入仍需使用以下完整文字：

| 指令 | 效果 |
| --- | --- |
| `去程`／`回程` | 今天現在之後最近三班，顯示查詢日期與現在時間 |
| `1`／`2`／`3` | 選擇剛剛列表的班次，顯示到達時間與乘車按鈕 |
| `已搭上` | 顯示所選班次的預計抵達時間，保留選車時開始的追蹤 |
| `搭上了` | 與「已搭上」相同；按鈕使用這個文字 |
| `沒搭上` | 排除所選班次，查詢下一班 |
| `說明` | 手動查看操作方式，沒有說明按鈕 |

「我想搭回程」「我已上車了」「回程選擇1」等整句聊天文字不會觸發，機器人也不會自動回說明。首尾空白會忽略，全形 `１` 可使用。


## 按鈕與雲端誤點追蹤

| 階段 | 按鈕（依序） |
| --- | --- |
| 開始／查詢說明 | 去程／回程／其他路線 |
| 查到三班 | 1／2／3／去程／回程／其他路線 |
| 選定車 | 知道／搭上了／沒搭上／停止追蹤 |
| 搭上了 | 知道／停止追蹤 |
| 沒搭上 | 知道／去程／回程／其他路線／停止追蹤 |
| 知道 | 不附按鈕；不影響追蹤 |

- 每個群組共用一個行程；每個私訊獨立。任何群組成員可操作，選另一班就換追另一班。數字對應群組最近一次列表；按鈕仍綁定原班次。
- 選定班次即建立追蹤。若距出發超過 30 分鐘，先等到出發前 30 分鐘再查即時資料；之後約每分鐘檢查。沒有有效追蹤時不排程。
- 誤點嚴格大於 4／9 分鐘才通知（整數資料即 5／10 分鐘），各一次。第一次取得資料已超過兩門檻時，兩個門檻通知會依序發出；恢復後再次超過不重複。同一仍在追蹤的車次重選會保留通知紀錄。
- 按「沒搭上」即停止舊車追蹤，成功查到下一班且 LINE 回覆成功後開始新追蹤。沒下一班或查詢失敗時不再追舊車；可重新查詢。
- 按「停止追蹤」或完整輸入同名指令立即取消。舊車停止按鈕不能取消新車。「知道」只確認閱讀，不停止。
- 預估抵達時間依表訂加最新可用誤點；資料缺少或過期不代表準點，維持最後有效 ETA（從未取得即時資料則採表訂）。抵達時再確認一次最新資料；有新延誤則延期，確認已到站或到 ETA 即結束，不再發送。這不是實際到站保證。
- 使用 Durable Object alarm 與持久快照，Worker 重啟仍可追蹤。推播前持久保存 LINE retry key 及內容，網路錯誤以相同 key 重試，避免重複通知。
- LINE Push 使用訊息額度；TDX 查詢及 Cloudflare 使用量依帳號方案計算。本次未開通或升級付費方案。
- 已移除群組管理者限制；Cloudflare 原有 GROUP_CONTROLLER_USER_ID Secret 不再讀取。為避免混用舊使用者專屬列表，本版首次部署後需重新傳「去程／回程／其他路線」。
- 原有本機 Node.js 入口仍可測試查詢，但自動追蹤的排程僅在 Workers 啟用。修改文案後需重新部署。

主要程式：src/tracking.mjs 的 DelayTracker.start/stop/poll 處理追蹤；src/worker.mjs 的 BotState.alarm/persist 將排程與快照一起儲存；src/line.mjs 的 push 沿用 runtimeFetch 與 redirect: manual。

可編輯文案：`boarded`、`boardedOtherRoute`、`delayNotice`、`trackingStopped`、`noTracking`、`buttonStopTracking`。門檻及查詢間隔由程式決定，修改顯示文字不會改變邏輯。
