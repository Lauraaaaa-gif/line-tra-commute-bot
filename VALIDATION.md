# 最新：2026-08-29 取消誤點通知

- 已移除追蹤排程、LINE Push、上車／停止通知指令及按鈕；僅保留明確查詢回覆。
- 69 項自動化測試通過；文案格式與人工預覽通過。包含舊按鈕無效、無背景 timer、保留即時選車估算及沒搭上流程。
- 實際 .env 憑證未修改；舊追蹤環境變數已不使用。
- 使用者明確同意資料經 ngrok 轉送後，取消通知版本已重新啟動；既有 Webhook URL 未更改，Use webhook 維持開啟。
- 公網 /health 200、敏感檔案路徑 404、錯誤簽章 401、合法空事件 200；LINE 官方 Verify 成功且回報 200。本次驗證沒有傳送聊天或推播，實際按鈕操作仍請使用者在 LINE 確認。
- 以下為歷史驗證紀錄；提到上車或推播的紀錄不代表目前功能。

# 驗證紀錄

## 2026-08-29 指定文案版本重新啟動成功

- 使用者在收到具體資料轉送風險說明後回覆「同意」，明確授權 LINE 訊息內容、使用者識別碼及 replyToken 經 ngrok 第三方伺服器轉送至本機後端；重新提出執行請求後獲准。
- 新版後端與 ngrok 已啟動，公開 Webhook 網址維持不變。四段指定文案已載入，誤點通知文案未修改。
- 公開 `/health` 回 200、敏感檔案路徑回 404、無效簽章回 401、有效簽章空事件回 200；LINE 官方 Verify 回 success=true、statusCode=200。
- 本次只做空事件連線驗證，未傳送聊天或推播訊息；使用者可重新傳「回程」測試新文案。

## 2026-08-29 四段指定文案更新

- 列表、已選班次、已上車、沒搭上四段文案已改為使用者提供的樣式，包含上車文案的全形逗號。日期、時間、車種、車號、站名與去回程方向皆由資料填入，沒有固定採用範例時間。
- 選班文案的 `05:44` 依指定樣式顯示為 `5:44`；其他抵達時間保留兩位小時。跨日會顯示到達日期。
- 沒搭上會排除已錯過班次及其之前的時刻，只顯示下一班；後續乘車按鈕對應該班，不會自動開啟追蹤。查無班次或查詢失敗時顯示提示，不捏造時間。
- 誤點通知的 `delayUpdate` 與其使用的 `eta` 等文案維持原樣，嚴格超過五分鐘、明確指令才啟用等規則未變。
- **77 / 77 自動化測試通過**，含四段文案、動態抵達時間、去程切換、沒搭上空班表／失敗、跨日，以及原有誤點通知文字與門檻測試。離線文案檢查與預覽通過。
- 檢查時舊後端與 ngrok 行程已不存在，3000 埠沒有服務監聽。嘗試啟動新版公開服務的工具請求被執行環境拒絕，要求明確授權 LINE 訊息內容、使用者識別碼及 replyToken 經 ngrok 第三方伺服器轉送。未繞過拒絕，尚未重新啟動公開服務；需取得授權後再啟動及 Verify。

## 2026-08-29 02:38 明確指令、可編輯文案與上車後誤點通知

- 數字按鈕改為 `1／2／3`；查詢顯示現在時間、距離發車及表訂抵達目的站的剩餘分鐘。選班後顯示到達時間與「上車了／沒搭上」。
- 僅完整指令或有效按鈕觸發；閒聊、不完整指令、舊別名與加入好友事件不自動回覆。選班不會自動追蹤，必須明確上車。
- 新增 `copy.zh-TW.json`、文案熱更新與 `文案修改教學.md`；儲存後下一則訊息使用新版，錯誤 JSON／不明變數保留上一有效版本。提供 `run.ps1 copy-check` 離線格式檢查／預覽。
- 上車後每 60 秒查詢；嚴格 `DelayTime > 5` 且分鐘數改變才主動通知。5 分鐘整不通知、6 才通知；降回門檻以下也不推播。到站、停止、沒搭上、超時或通知上限時不另外推播結束訊息。
- 誤點資料取 TDX v3 TrainLiveBoard；到達估計為表訂＋目前誤點，並明確註明非保證。接近發車前 30 分鐘才採即時資料；過期、缺欄位、錯日期資料不視為準點。
- **75 / 75 自動化測試通過**：包括真正 localhost 簽章 HTTP → 選班 → 上車 → 即時 5 不推／6 才推 → 停止；LINE 上游為 mock。另有實際定時器、同一使用者事件順序、群組／使用者隔離、推播重試 key／409、停止取消重試、熱更新與錯誤文案測試。
- 真實 TDX 全體 TrainLiveBoard 與指定車次端點均回 HTTP 200，確認 DelayTime、位置及 UpdateTime 欄位存在；真實回程三班班表查詢成功。
- 三種 reply 內容（列表、選班、上車確認）及一種 push 誤點格式，送 LINE 官方 validate API 均回 HTTP 200。推播格式驗證使用人工 6 分鐘誤點，**未用测试腳本發送真實聊天或推播，也未宣稱真實列車誤點 6 分鐘**。
- 已重啟新版後端／ngrok，網址不變；公網健康檢查 200、敏感檔案 404、無效簽章 401。LINE 官方 Verify 回 success=true/statusCode=200，Use webhook 為開啟。
- 真實搭車期間收到誤點推播的端到端體驗仍待使用者確認。Push 使用帳號訊息額度，程式不購買額度或更改方案。TDX 用量、網路、電腦休眠與上游資訊新鮮度都影響可用性。
- 追蹤收件者 ID 僅存在記憶體，不存檔／不記錄日誌；重啟清空，不會自動恢復追蹤。實際 `.env` 僅新增三個追蹤設定，既有設定及憑證逐项比對保持不變。

## 2026-08-29 班次到達時間、三班列表與按鈕調整

- 使用者截圖及回報已確認去程／回程能在 LINE 正常取得班表。
- 新增班次 postback 按鈕及數字選擇；回覆選定班次的目的站預定到達時間。保留原列表，不重新查 TDX；使用者／聊天室隔離，30 分鐘、換日或重啟後失效。
- 依使用者要求移除「說明」按鈕，只保留「去程／回程」導覽。實際 `.env`、預設設定與範例均改為 `RESULT_LIMIT=3`；其他設定與所有金鑰保持不變。
- **57 / 57 自動化測試通過**，含完整 HTTP 簽章 → 查詢 → postback → 到達時間回覆、數字選擇、舊按鈕、無效序號、跨使用者／聊天室、到期／換日、容量淘汰、LINE 失敗不覆蓋列表及跨午夜到站日期。
- 真實 TDX 實測：2026-08-29 新左營 → 大湖，區間車 3118，05:20 出發、05:44 預定到達；大湖 → 新左營，區間車 3111，05:40 出發、06:05 預定到達。以上為當次班表，非即時誤點。
- 兩方向的列表及到達時間訊息，送 LINE 官方 `POST /v2/bot/message/validate/reply`，四次均回 HTTP 200。只做格式驗證，未以測試腳本發送聊天訊息。
- 已停止本專案舊後端與 ngrok，啟動新版；公開 Webhook 網址維持不變，讀取確認 Use webhook 已開啟。公開健康檢查 200、敏感檔案路徑 404、無效簽章 401；LINE 官方 Verify 再次回 success=true、statusCode=200。
- 使用者須重新傳「回程」取得新版三班列表，再點「1 到達時間」或輸入 `1`；實際新版點選回覆尚待使用者確認。

## 2026-08-29 01:35 公開 HTTPS 與 LINE 官方 Verify 成功

- 使用者明確同意 LINE Webhook 的訊息內容、使用者識別碼及回覆用 Token 經 ngrok 第三方伺服器轉送，並授權啟動公開 HTTPS Webhook；重新提出工具執行請求後獲准。
- `scripts/share.mjs` 已啟動本機 `127.0.0.1:3000` 後端及 Microsoft Store 版 ngrok，公開網址為 `https://kettle-calcium-elevating.ngrok-free.dev`。本次保持行程執行，尚未停止。
- 外部 HTTPS `/health` 回 HTTP 200 且服務識別正確；`/.env` 與 `/src/server.mjs` 均回 404；錯誤 Webhook 簽章回 401、本機簽章空事件回 200。
- LINE 官方 `POST /v2/bot/channel/webhook/test` 指向公開 `/webhook`，回 `success: true`、`statusCode: 200`；已確認目前的 Channel secret 與官方帳號配對正確。
- LINE Token 對應「小小栗（通勤小助手）」，Bot basic ID 為 `@232ekcrn`。確認原先沒有 Webhook 後，透過官方 API 儲存公開 `/webhook` 網址；再次讀取確認網址一致。
- 最後讀取 `active: false`，仍需使用者於 LINE Developers 啟用 **Use webhook**。本次只發送空事件驗證，未傳送聊天訊息；實際 LINE 指令 → TDX → replyToken 回覆仍待手機實測。

## 2026-08-29 01:19 ngrok 金鑰格式修正與公開啟動準備

- 讀到 `NGROK_AUTHTOKEN` 被填成完整的 `ngrok config add-authtoken …` 指令。僅用嚴格格式比對抽取其中的金鑰，未執行貼入內容；儲存後逐項確認其他設定完全保留，未輸出任何金鑰。
- 新增 `scripts/share.mjs`、`src/ngrok.mjs`、不含金鑰的 `ngrok.local.yml` 及 VS Code 公開 Webhook 啟動選項。預設仍為離線示範；新增啟動工具一併管理本機後端與 ngrok，並只向 ngrok 子行程提供必要系統環境及自己的 Authtoken。
- 新增 5 項 ngrok 設定／環境隔離／安全日誌測試，合計 **49 / 49 測試通過**。`share.mjs --check` 通過；不帶公開確認參數時會停止並回 `PUBLIC_WEBHOOK_CONFIRMATION_REQUIRED`。
- 使用已安裝的 ngrok 執行離線 `config check` 成功。本次尚未驗證 Authtoken 的伺服器端有效性。
- 真正啟動公開連線的工具請求在建立行程前被拒絕：執行環境要求使用者明確授權 LINE Webhook 資料經第三方 ngrok 伺服器轉送。未改用其他方式執行拒絕的動作。
- 確認尚無 `.runtime/share.json`；本次未建立公開網址、未修改 LINE Webhook，也未向 LINE 傳送訊息。公開連線與完整 LINE Verify／收發測試仍待授權後進行。

## 2026-08-29 ngrok Microsoft Store 版安裝確認

- 使用者完成商店安裝後，確認已存在 `ngrok.ngrok` 套件，執行版本查詢成功：`3.39.9-msix-stable`。
- 使用的命令別名為 `C:\Users\z0117\AppData\Local\Microsoft\WindowsApps\ngrok.exe`，未使用先前被 Windows 安全性攔截的直接下載版。
- 重新讀取 `.env`，第 31 行 `NGROK_AUTHTOKEN` 仍為空白；LINE／TDX 四項憑證均保留且有值。此步未修改任何憑證。
- 本次只執行 ngrok 版本查詢，未啟動 agent、公開後端或傳送 LINE 訊息。待使用者在本機填入 Authtoken 並儲存後，才能進行連線驗證。

## 2026-08-29 00:51 ngrok 本機設定準備

- 使用者已登入 ngrok，準備取得 Your Authtoken；僅在 `.env` 末尾新增空白 `NGROK_AUTHTOKEN` 欄位，並確認所有既有 LINE／TDX 憑證與其他設定完全保留。`.env.example` 同步提供選用欄位；填值本身不會啟動 tunnel。
- 本機未找到 ngrok 或 winget 命令，也沒有標準位置的 ngrok 設定檔。
- 經許可從 ngrok 官方 Windows 頁面的下載連結取得 x64 版本；嘗試檢查發行者簽章時，Windows 安全性回報「檔案包含病毒或潛在的垃圾軟體」，未執行該檔案或啟動 ngrok。
- 未停用防護、未新增排除、未還原被攔截檔案；已移除這次下載的暫存壓縮檔。改請使用者透過 ngrok 官方建議的 Microsoft Store 版本安裝。
- ngrok 尚未安裝完成、Authtoken 尚未填入或驗證，公開 HTTPS Webhook 與實際 LINE 收發仍待完成。

## 2026-08-29 00:37 LINE Token 與本機後端驗證成功

- 重新讀取使用者儲存的 `.env`，四個憑證欄位均已填寫，各一筆設定，沒有空白、遮罩或範例文字，也沒有被繼承的環境變數覆蓋；未修改或輸出憑證。
- 使用 LINE 官方唯讀 `GET /v2/bot/info`，回 HTTP 200，確認 Messaging API Token 有效；官方帳號名稱為「小小栗（通勤小助手）」。
- 唯讀 `GET /v2/bot/channel/webhook/endpoint` 回 HTTP 404；本次未設定或修改 LINE Webhook，也未傳送任何 LINE 訊息。
- 再次實際呼叫 TDX：認證、車站，以及大湖 → 新左營／新左營 → 大湖的當日 OD 班表均回 HTTP 200。
- 使用實際 `.env` 啟動 `src/server.mjs`，只將 HOST／PORT 覆寫為本機回環與臨時測試埠；健康檢查回 200、本機用已填 Secret 簽章的空事件回 200、錯誤簽章回 401。測試子行程已停止。
- 上述本機簽章測試不代表 Channel secret 已與 LINE 官方帳號配對成功；仍需公開 HTTPS Webhook 接收 LINE 官方的 Verify 請求，再用 LINE 傳訊息驗證完整流程。
- 重新執行 44 項自動化測試全部通過。未找到可用的 ngrok／cloudflared 命令或標準位置的 ngrok 設定；尚未安裝或啟動 tunnel、尚未公開本機服務。

## 2026-08-29 00:21 真實 TDX 查詢成功

- 重新讀取使用者最新儲存的 `.env` 後，TDX 認證端點回 HTTP 200，成功取得 Token；未輸出金鑰或 Token。
- 車站 API 回 HTTP 200，確認大湖為 `4290`（高雄市路竹區）、新左營為 `4340`。
- 2026-08-29 去程與回程的 OD 班表 API 均回 HTTP 200，已用真實資料完成現在之後的最近班次查詢。
- 台灣時間 00:21 查詢結果：大湖 → 新左營為 05:40、06:03、06:31、06:59；新左營 → 大湖為 05:20、05:44、06:11、06:32。這是當次時刻表結果，非即時誤點資訊。
- 當時 `LINE_CHANNEL_SECRET` 與 `LINE_CHANNEL_ACCESS_TOKEN` 尚未填寫；該次未傳送 LINE 訊息，也未啟動公開 Webhook。

## 2026-08-29 設定更新與先前測試

- VS Code 的三個偵錯設定與測試工作已改為使用本機 Node.js 的明確路徑；移除 workspace 內重複設定，統一使用 `.vscode` 設定。
- 實際 `.env`、預設路線、示範與測試改為大湖 → 新左營／新左營 → 大湖；既有憑證與其他環境設定未改動。
- 路線修改後重新執行 44 項測試，全部通過；離線示範正常顯示「新左營 → 大湖」，仍明確標示為人工假資料。
- 較早使用 `.env` 內的 TDX 憑證嘗試連線時，認證端點回 HTTP 401；當時未取得車站及班表資料，也未傳送 LINE 訊息。
- 先前一次重新儲存後仍為 HTTP 401。當時額外繞過專案客戶端，直接依官方 client credentials 格式送出認證請求，回傳 `unauthorized_client`。本機檢查確認兩個欄位均有值、各一筆設定，沒有空白、非 ASCII 字元、遮罩／範例文字，也沒有被繼承的環境變數覆蓋。未輸出或修改金鑰；此認證問題已在上方最新實測中排除。
- 大湖站身分已依台鐵官方車站頁面確認；TDX 正式查詢仍會動態查站名。

## 2026-08-28 初始驗證

- 日期：2026-08-28（Asia/Taipei）
- 環境：Windows，Node.js v24.19.0
- `node --test --experimental-test-coverage`：**44 / 44 測試通過**，0 失敗、0 略過。
- 真正啟動 `src/server.mjs` 子行程後，localhost `/health` 與帶正確簽章的空事件 Webhook 均回 HTTP 200。測試行程已停止。
- `node scripts/demo.mjs` 與 `run.ps1 demo` 均成功，顯示明確標示的人工示範班表。
- 未填金鑰時，後端與真實查詢腳本均會停止並列出缺少的變數名稱，不會改用假資料。
- VS Code workspace、launch、tasks 及 package.json 皆通過 JSON 解析；本機 workspace 指定的 Node 執行檔存在。

## 測試包含

去回程路線、台灣日期切換、現在之後的班次、以離站時間篩選、停駛與車種、分頁、快取、TDX token 到期與 401 更新、429、timeout、錯誤 JSON、HMAC 驗章、身分與金鑰不寫入錯誤、事件去重、部分失敗後重送、並行處理限制、過大分塊 body，以及完整 localhost Webhook → 模擬 TDX → 模擬 LINE 回覆流程。

## 尚待設定後驗證

TDX、LINE Token、公網 HTTPS 與官方 Verify 均已成功；使用者已確認實際去回程查詢能回覆。新增到達時間功能已完成自動化測試及官方格式驗證，仍請使用者確認新版 LINE 點選體驗。自動化 HTTP 串接測試中的上游回應仍為人工資料，與上方真實 API 實測分開記錄。公開服務依賴本機後端與 ngrok 持續執行，關機／休眠後不可用。

Dockerfile 已提供，但未在此環境實際建置。單一實例的記憶體去重不保證跨重啟或多主機只處理一次；跨午夜列車範圍與其餘限制見 README。
