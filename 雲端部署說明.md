# 電腦關機也能使用：雲端部署

目前尚未部署雲端。使用者不接受付費主機，已取消誤點追蹤、每分鐘排程及主動推播；不會建立付費資源。

## 目前架構

- 保留去回程最近三班、選車看抵達時間、沒搭上找下一班。
- 只有收到明確指令時才執行；沒有背景定時工作或 LINE Push。
- 仍需一台能執行 Node.js 24 / Docker、接收公開 HTTPS Webhook 的主機。本機關機時無法使用。
- 免費、會休眠的 Node.js 服務可列入評估，但喚醒時首則訊息可能逾時或無法回覆，不能保證即時可用。
- 目前選擇與事件去重存於記憶體。只使用 1 個副本；休眠後若程序重啟，或重新部署，請重新傳去程／回程查詢。

## 部署前

確認平台、免費方案限制及登入方式後才部署。新平台會接收 LINE 訊息、使用者識別碼與 replyToken，並保存執行需要的 LINE／TDX 金鑰，轉移前需確認同意。

程式檔案只需 Dockerfile、package.json、copy.zh-TW.json、src/ 及 .dockerignore。不要上傳 .env、.runtime/、ngrok.local.yml 或 ngrok 金鑰。.dockerignore 只影響 Docker 建置，不代表其他上傳工具也會排除敏感檔案。

## 服務設定

部署目錄為 line-tra-bot，使用 Dockerfile，或 Node.js 24 執行 node src/server.mjs。沒有外部 npm 套件。

| 設定 | 值 |
| --- | --- |
| HOST | 0.0.0.0 |
| PORT | 使用平台分配值；未分配時 3000 |
| NODE_ENV | production |
| RESULT_LIMIT | 3 |
| 健康檢查 | /health |
| 副本數 | 1 |

平台秘密環境變數：LINE_CHANNEL_SECRET、LINE_CHANNEL_ACCESS_TOKEN、TDX_CLIENT_ID、TDX_CLIENT_SECRET。不要填入程式碼、Git 或對話，不需 NGROK_AUTHTOKEN。預設路線已是大湖 → 新左營及回程。

## 切換順序

1. 部署完成，取得公開 HTTPS 網址。
2. 驗證 /health 為 200、錯誤簽章 /webhook 為 401、/.env 及 /src/server.mjs 為 404，合法空事件為 200；確認 TDX 可用。
3. 確認切換後，在 LINE Developers 把 Webhook URL 改為雲端網址 + /webhook，按 Verify，保持 Use webhook 開啟。
4. 真實 LINE 查詢成功後才停止本機後端與 ngrok。
5. 電腦關機後用手機查詢，另驗證服務閒置後第一次查詢的喚醒行為。

若失敗且本機仍運作，可改回原本的 ngrok URL 並 Verify。不要尚未驗證就停掉可用入口。

## 文案與驗證限制

在 VS Code 修改 copy.zh-TW.json、驗證後重新部署，才會改變雲端文案。

本機沒有 Docker，尚未實際建置容器；未建立雲端帳號、上傳金鑰或切換 Webhook。免費主機部署與休眠喚醒驗證仍待完成。
