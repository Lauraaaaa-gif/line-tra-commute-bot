# Cloudflare Workers 雲端部署

此版本使用 Cloudflare Worker 接收 LINE Webhook，並使用 Durable Object 保存每個聊天室最近 30 分鐘的班次與選車狀態。電腦關機後仍可使用，也不需要 ngrok。

## 不公開金鑰

下列五項一律設定成 Cloudflare **Secret**，不能寫入 `wrangler.jsonc`、GitHub、README 或聊天內容：

- `LINE_CHANNEL_SECRET`
- `LINE_CHANNEL_ACCESS_TOKEN`
- `TDX_CLIENT_ID`
- `TDX_CLIENT_SECRET`
- `GROUP_CONTROLLER_USER_ID`

`.env` 已被 `.gitignore` 排除，只供本機使用。`NGROK_AUTHTOKEN` 不會傳到 Cloudflare。

Wrangler 設定 Secret 時會在終端機中個別提示輸入，不需要把值放在指令中：

```powershell
npx wrangler secret put LINE_CHANNEL_SECRET
npx wrangler secret put LINE_CHANNEL_ACCESS_TOKEN
npx wrangler secret put TDX_CLIENT_ID
npx wrangler secret put TDX_CLIENT_SECRET
npx wrangler secret put GROUP_CONTROLLER_USER_ID
```

## 架構

- `src/worker.mjs`：Worker HTTPS 入口，處理 `/health` 與 `/webhook`。
- `BotState` Durable Object：依聊天室隔離並保存班次、選車與 Webhook 去重狀態。
- 聊天室 ID 先做 SHA-256 摘要才用於 Durable Object 名稱。
- LINE 簽章先對原始 Webhook bytes 驗證，通過後才解析 JSON。
- 沒有誤點背景追蹤、排程或 LINE Push。

## 部署設定

`wrangler.jsonc` 只包含公開、非敏感設定：大湖／新左營路線、三班結果、逾時與 Durable Object binding。文案來自 `copy.zh-TW.json`，修改後需重新部署。

部署前執行：

```powershell
npm test
npm run copy:check
npx wrangler deploy --dry-run
```

設定完五項 Secrets 後部署：

```powershell
npm run deploy:worker
```

部署成功會取得 `https://line-tra-commute-bot.<帳號>.workers.dev`。LINE Webhook URL 使用：

```text
https://line-tra-commute-bot.<帳號>.workers.dev/webhook
```

## 安全切換順序

1. 先部署 Worker，不變更目前的 LINE Webhook。
2. 確認 `/health` 回傳 200。
3. 在 LINE Developers 將 Webhook URL 改成 Workers URL 加 `/webhook`，按 **Verify**。
4. Verify 成功後保持 **Use webhook** 開啟。
5. 用 LINE 實測「回程 → 1 → 搭上了／沒搭上 → 知道」。
6. 群組中再確認只有管理者能控制，家人可以看到資訊並按「知道」。
7. 全部成功後才停止或刪除 Render 服務；保留 Render 可作為短期回復入口。

如果 Workers 實測失敗，立即把 LINE Webhook 改回原本的 Render URL 並按 Verify，不需要公開或更換任何金鑰。
